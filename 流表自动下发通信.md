构建oar的仓库：[multi-modal-network/Basic-tna (github.com)](https://github.com/multi-modal-network/Basic-tna)

工具仓库：[multi-modal-network/p4rt-modal-test (github.com)](https://github.com/multi-modal-network/p4rt-modal-test)

## ONOS

拉取onos官方提供的仓库

```
git clone https://github.com/opennetworkinglab/onos.git
```

直接运行onos

```
bazel run onos-local -- clean debug  # 需要提前安装bazel，如果安装失败尝试搭建代理科学上网，多重试几次
```

访问http://ip:8181/onos/ui访问onos网页

## Mininet

执行以下命令，使用mn-stratum-ovs-scapy镜像来构建docker容器

```
sudo docker run --privileged -itd -v /lib/modules:/lib/modules -v /home/onos/Desktop:/home/onos/Desktop --network=host --name mn-stratum mn-stratum-ovs-scapy  # 需要将/lib/modules和/home/onos/Desktop目录持久化映射到docker容器中
```

进入docker容器，启动mininet

```
sudo docker exec -it mn-stratum /bin/bash
# 进入工具仓库的mininet目录
cd /home/onos/Desktop/p4rt-modal-test/mininet
# 启动mininet的python脚本
mn --custom mm_topo.py --topo tutorialtopo --arp
```





## 流程

- Basic-tna库中编译**多模态oar**（仓库中已经有编译好的oar了，在）

  ```
  sudo make pipeconf   # 可能编译失败，报错"Temporary failure in name resolution"，排查dns问题
  sudo make pipeconf-install ONOS_HOST=localhost    # 上传oar至控制器，直接启动
  ```

- onos应用启动**bmv2 drivers**

- 通过onos API上传network configuration信息

- 进入docker容器中启动generate.py脚本

- 在mininet命令行中使用h164~h200中的任意两台主机进行收发包测试

  ```
  # h180向h166主机发送id模态的数据包，发送时长1秒，频率为2packet/s
  h166 cd /home/onos/Desktop/p4rt-modal-test/mininet
  h166 nohup python3 receive.py > recv_log.out &			# h166是目的主机
  h180 cd /home/onos/Desktop/p4rt-modal-test/mininet
  ## send.py [modelType 模态类型] [total_time 发送时长] [frequency 发送频率] [source_host 源主机] [destination_host 目的主机]
  h180 nohup python3 send.py 'id' 1 2 'h180' 'h166' > send_log.out &      # h180是源主机
  ```

- 如果recv_log.out中出现了”got a XXX packet“，说明目的主机收到了源主机发送的数据包

  - 此时generate.py脚本会输出流表下发成功的success日志信息
  - 去onos页面查看流表，已下发成功，然后观察h180~h166沿途的交换机内都有配置下发的流表，并且有数据包转发的记录



